name: Build and Deploy FE

on:
  push:
    branches:
      - hasontech   # chỉ chạy khi push lên nhánh main
permissions:
  contents: write
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v3

      - name: Setup NodeJS
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install
        working-directory: ./Frontend

      - name: Build project
        run: npm run build
        working-directory: ./Frontend

      - name: Deploy to branch
        run: |
          rm -rf .git
          mkdir deploy_hasontech
          # copy nguyên dist folder
          cp -r Frontend/dist deploy_hasontech/
          # copy Dockerfile và nginx.conf
          cp Frontend/Dockerfile deploy_hasontech/
          cp Frontend/nginx.conf deploy_hasontech/

          cd deploy_hasontech
          git init
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Deploy FE"
          git branch -M deploy_hasontech
          git remote add origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
          git push -f origin deploy_hasontech
